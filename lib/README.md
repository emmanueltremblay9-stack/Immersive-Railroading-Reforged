# Local Runtime JAR Mirror

`install-mod.ps1` always mirrors the built Immersive Railroading Reforged runtime JAR and required runtime dependency JARs into this directory.

The copied `.jar` files are local build/install artifacts and are intentionally ignored by Git.
